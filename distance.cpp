#include <bits/stdc++.h>
using namespace std;
const int N = 1e7;
bool visited[N];
int main()
{
    int n, m;
    cin >> n >> m;
    int arr[m][2];
    for (int i = 0; i <= m; i++)
    {
        for (int j = 0; j < 2; j++)
        {
            cin >> arr[i][j];
        }
    }
    
}