#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        long arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        sort(arr, arr + n);
        int x[n];
        for (int i = 0; i < n; i++)
        {
            x[i] = -arr[i];
        }
        for (int i = 0; i < ; i++)
        {
            /* code */
        }
        
    }
}