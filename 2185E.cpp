#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n,m,k;
        cin>>n>>m>>k;
        vector<long long>robot(n),spike(m);
        for(auto &a : robot)
        cin>>a;
        for(auto &a : spike)
        cin>>a;
        string s;
        cin>>s;
       for (int i = 0; i < k; i++)
       {
            
       }
       
    }
}